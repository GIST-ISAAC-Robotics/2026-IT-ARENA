# 단일 차량 저속 시연용. 실물 차량에 바로 적용하지 않습니다.
wall_follow:
  ros__parameters:
    target_wall_distance_m: 0.425
    max_speed_mps: 0.35
    min_speed_mps: 0.14
    scan_timeout_s: 0.45
    image_timeout_s: 1.0
    control_rate_hz: 20.0
