# 단일 모터·차동·ToF 검증 자료

2026-08-31의 2.000 kg 기준 차량에서 나온 대표 실행 보고서를 고정 이름으로 복사한 묶음입니다. 원시 궤적·로그·실행별 임시 YAML/SDF는 반복 생성물인 `artifacts/tests/`에 남기며 Git에는 넣지 않습니다. 각 JSON의 `input_sha256`와 `started_at_utc`로 입력과 실행을 구분합니다.

## 요약

| 시험 | 대표 결과 | 해석 한계 |
|---|---|---|
| 브리스크 본선 한 바퀴 | 48.6527 m 진행, 고정 외형-정적 벽 겹침 0회, 최대 중심선 거리 0.1301 m, 최고 실속도 0.8005 m/s, 실제 정지·정상 종료 | 단일차·실험 지도·약 0.7 m/s 데모이며 노면 경계·다중 차량·고속 경주 검증 아님 |
| 평지 직선 20 km/h | 정상상태 중앙값 19.997 km/h, 순간 최고 20.559 km/h, 명령 중단 뒤 실제 정지 | 모터·타이어 값이 임시이며 대회 트랙에서의 안전 속도 아님 |
| 5 km/h 코너 | 정상상태 4.992 km/h, 실제 중앙 회전반경 0.394 m, 뒤축 횡슬립 근사 최고 0.018 m/s | 조향 명령 0.37 rad의 독립 평지 원주 시험 |
| 8 km/h 코너 | 실제 중앙 회전반경 약 0.628 m, 뒤축 횡슬립 근사 최고 약 0.038–0.042 m/s | 기하 반경 약 0.38 m보다 크게 선회한 언더스티어 관측. 타이어 실측 전 |
| 편측 저마찰·오픈 차동 | 좌우 최대 속도차 149.51 rad/s, 동일 토크. 명령 종료 3 s 뒤 바퀴 표면 0.012 m/s이나 차체 0.606 m/s로 이동 | 의도적인 극저마찰 민감도 시험이며 일반 정지 통과 아님 |
| 편측 저마찰·점성 LSD 근사 | 정상상태 약 2.000 m/s, 최대 차동 토크차 0.060 N·m, 실제 정지 | 특정 제품의 LSD 성능·내구성 검증 아님 |
| 전방 ToF 정지 | 높이 5 cm 정적 표적, 최고 1.090 m/s, 최소 차체 여유 0.195 m, 비접촉 정지 | 평지 정적 표적·이상화 점군이며 상대속도 TTC 없음 |
| 후방 ToF 정지 | 높이 5 cm 정적 표적, 최고 0.799 m/s, 최소 차체 여유 0.224 m, 비접촉 정지 | 후진 조향 0의 단일 표적 시험 |
| 20 km/h 코사인 요철 | 피치 최고 25.79°, 차체 기준점 높이 최고 0.1177 m, 전복 없음 | 20 cm 길이·1 cm 높이의 임시 강체 요철, 실제 과속방지턱 사양 아님 |
| 20 km/h 편측 4 cm 턱 | 롤 최고 4.88°, 피치 13.02°, 큰 휠 속도차·슬립, 전복 없음 | 대회 시설이 아닌 충돌 민감도 표적 |
| 고 CG·고마찰 민감도 | 명목 CG를 약 10.18 cm로 높인 두 시험 모두 전복 없음 | CAD 질량 분포가 아니라 인위적 관성 이동 시험. 실제 전복 임계값을 뜻하지 않음 |

8 km/h 코너를 물리 시간 간격 1.0 ms와 0.5 ms로 비교했을 때 실제 중앙 회전반경은 각각 약 0.628 m로 같았습니다. 이는 해당 관측의 시간 간격 민감도가 작다는 한 가지 증거일 뿐, 전체 차량 모델의 수렴 검증은 아닙니다.

## 파일

- `brisk_one_lap.json`: 현재 단일 모터·ToF 보호층·센서 벽 추종 통합 한 바퀴.
- `straight_20kmh.json`, `corner_5kmh.json`, `corner_8kmh_step_*.json`: 평지 속도·선회·시간 간격 비교.
- `split_grip_open.json`, `split_grip_viscous_lsd.json`: 편측 저마찰 차동 비교.
- `tof_forward_stop.json`, `tof_reverse_stop.json`: 낮은 정적 표적 제동.
- `bump_20kmh.json`, `curb_trip_20kmh.json`, `high_cg_*.json`: 물성·접촉 민감도.

각 위험 시험의 `passed: true`는 요청한 계측이 끝나고 프로세스가 정상 종료됐다는 뜻입니다. 모든 파일의 `safe_racing_verified`는 `false`입니다. `rolled_over: false`도 전복을 물리적으로 불가능하게 만들었다는 뜻이 아니라 이번 조건에서 60° 임계각을 넘지 않았다는 결과입니다.
