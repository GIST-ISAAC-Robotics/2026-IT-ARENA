# 공식 `v2026.09.02` 전환 검증 근거

검증일: 2026-09-02
공식 태그: `v2026.09.02`
공식 커밋: `cb8fc14b1027c956b04cc297fa1454a65c956bfb`

## 범위

공식 ZIP은 수정하지 않고 `assets/track/official/v2026.09.02/`에 보존했다.
원본 로드·형상과 팀 파생 월드의 정적 경로, 마커·신호등 RGB, 방지턱,
차량 센서·조향·정지를 서로 구분해 검사했다. 지름길 연속 진입·합류,
다중 차량·추월, 20 km/h 완주와 실물 센서 성능은 이 묶음의 범위가 아니다.

## 원본 재감사 요약

- ZIP 항목 27개·파일 24개, 경로 이탈·중복 0개
- `world.sdf` 엄격 검사 통과
- 첫 물리 갱신 종료 코드 0, 약 6.52초
- 물리 2,000회 종료 코드 0, 약 21.38초
- 노면 누락 약 0.0117 m²(0.0538%), 최대 공백 1.0 mm
- 잔디 누락 0.2311%, 노면 침범 0.1305%

기계 판독용 요약은 [`raw_release_audit.json`](raw_release_audit.json)에 있다.

이전 `v2026.09.01`의 큰 급커브 노면·갈림길 잔디 공백은 해소된 것으로
판정했다. 세부 근거와 공식/임시 구분은
[`docs/track/OFFICIAL_V2026_09_02_REAUDIT.md`](../../../../docs/track/OFFICIAL_V2026_09_02_REAUDIT.md)에 있다.

## 팀 경사 마커와 신호등

`facility_visibility_final/`은 실제 차량 RGB를 사용한 최종 정지 표본이다.

- 신호등: 6/6 통과
- 코스 ArUco: 12개 중 11개 통과
  - ID 0·20·45: 1.50·1.00·0.75 m 모두 검출
  - ID 30: 1.50·1.00 m 검출, 0.75 m는 급커브에서 판 전체가 화면 밖

`marker_visibility/`은 공식 PBR 판만 기울였을 때의 실패를 보존한다.
외곽 판이 큰 사각 후보가 되어 12개를 모두 놓쳤다. 잘린 코드를 정규화하면
ID가 검출되어 형상보다 렌더링 후보 분리 문제로 좁혔다.
`marker_overlay_capture/`은 공식 판 정면에 같은 0.2 mm SDF 패턴을 겹친
뒤 ID 0·45가 초기 화면에서 검출된 짧은 확인이다. 이후 전체 12조건을
위 최종 폴더에서 다시 실행했다.

정지 표본의 목표 pose는 검사기에만 전달했다. 대회용 자율주행 노드는
정답 위치나 마커 pose를 입력으로 읽지 않는다.

## 방지턱

세 번의 실행을 지우지 않고 원인과 함께 보존한다.

| 폴더 | 결과 | 원인·의미 |
|---|---|---|
| `bump_only/` | 실패 | 센서 상태가 준비되기 직전 60초 초기 제한 종료 |
| `bump_only_retry/` | 실패 | 깊이를 껐지만 7 sim s 주행에 50초 벽시계 한도가 현재 RTF보다 짧음 |
| `bump_only_retry2/` | 통과 | sim 시간 판정은 유지하고 벽시계 한도만 180초로 보정 |

최종 결과:

- 명령 속도: 0.16 m/s
- 전진 거리: 1.136265 m
- 차체 기준 높이: 0.002998 → 최대 0.008058 → 최종 0.003000 m
- 노란 띠 픽셀: 3,323
- 명령 중단 뒤 0.5 sim s 연속 0.01 m/s 미만 정지 통과
- 종료 코드 0/0, 오류 없음, 정상 종료

이는 단일 차량 저속 1회다. 고속 접촉·타이어 물성·실물 서스펜션과
전복 임계값을 검증하지 않는다.

## 센서·차량 스모크

[`smoke_low_load_30.json`](smoke_low_load_30.json)은 다음을 통과했다.

- RGB·깊이 848×480 약 30.303 sim Hz
- 여섯 ToF 8×8 약 15.1515 sim Hz
- 깊이 유효 범위 약 0.195~2.962 m
- IMU, 좌우 뒷바퀴 엔코더, 직진·조향
- 명령 중단 watchdog 정지와 정상 종료

주기는 시뮬레이션 시간 기준이다. 당시 벽시계 RTF는 약 0.105였으므로
실시간 30 Hz 처리 성능으로 표현하지 않는다.

## 후방 식별 마커

후면의 5×5 cm `DICT_4X4_50` ID 10은 패턴·치수·높이·충돌체 부재를
자동 검사했다. 질량·충돌체가 없는 팀 임시 시각 요소이며 다른 차량의
주행 영상에서 검출한 결과가 아니다.

## 본선 한 바퀴

최신 공식판에서 `brisk` 본선 한 바퀴+약 2 m를 통과했다. 원본 보고서는
[`brisk_one_lap_run/report.json`](brisk_one_lap_run/report.json), 요약본은
[`brisk_one_lap.json`](brisk_one_lap.json)에 보존한다.

- 진행 거리 48.6927 m, 판정된 한 바퀴 46.6329 m
- 최고 지면 속도 0.789711 m/s
- 최대 중심선 거리 0.128371 m
- 고정 20×15 cm 외형과 정적 벽 겹침 표본 0개
- 조기 출발 없음, 코스 마커 ID 0/20/30/45와 좌·우 벽 선택 모두 관측
- RGB·라이다·시계와 ToF 보호층 입력을 사용했고 정답 위치는 제어기에 주지 않음
- 라이다 500점, 약 10.0002 sim Hz, 0.05~12 m
- 종료 전 실제 정지, 최종 오도메트리 약 0.000038 m/s
- 종료 코드 0/0, 오류 없음, 정상 종료

이는 단일 차량의 저속 본선 완주다. 지름길, 다른 차량, 추월·회피,
20 km/h 마커 검출과 실물 센서·물성 검증으로 확대하지 않는다.

## 최종 정적·자동 검사

완주와 화면 촬영 뒤의 최종 소스에서 다음을 통과했다.

- ROS 패키지 5개 `colcon build --symlink-install`
- Python/ROS 검사 117개
- C++ 단일 모터·차동 검사 1개, 무작위 동력 관계 20,000회
- 공식 생성물 29개와 배포 ZIP 24개 파일의 해시·재현성 검사
- `experimental` 원본 23개 파일 보존·생성 해시·정적 경로 검사
- 공식·실험·초기 원본 재현 월드의 엄격한 SDF 검사
- 다각도 캡처 스크립트 문법 검사와 실제 다섯 장 실행·정상 종료

공유 시설 생성기 변경 뒤 첫 `experimental --check`는 이전 파생 출력의
`facility_builder_sha256`가 달라 의도대로 중단됐다. 현재 설정으로 실험
출력을 재생성한 뒤 재현성·SDF·전체 검사를 다시 통과했다. 공식 보존 ZIP과
과거 활동·검증 파일은 변경하지 않았다.
