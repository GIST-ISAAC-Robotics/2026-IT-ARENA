# 프로젝트 현황과 인수인계

최종 갱신: 2026-08-31

## 계속 확인할 자료

- [2026-06-30 미팅](https://maddening-cause-ce7.notion.site/2026-06-30-38f99fd42e3080f6956fe5a5b90d0824): A-1 도로 폭, A-2 차량 크기·지름길·ArUco 설계 의도. 2026-08-30 공개 본문을 확인한 과거 기록이며 최신 규정으로 단정하지 않습니다.
- 트랙 폭·차량 외형·표지판 배치에 관한 작업에서는 위 원문과 이후 주최 측 자료를 재확인합니다. 원문 접근이 안 되면 `docs/track/TRACK_AUDIT.md`의 확인 날짜·근거와 미확정 상태를 유지합니다.
- 현재 실행·원본 선택·재생성 방법과 차량 치수 근거: [실험 트랙 안내](track/EXPERIMENTAL_TRACK.md).
- 센서 명목 사양·고속/저부하 선택·실물과의 차이: [D435i 설정 안내](sensors/D435I_SIMULATION.md).
- 신호등·곡선 방지턱·독립형 표지판·출발 및 피니시 표시와 실제 영상 검사: [시설 안내](track/FACILITIES.md), [결정 0005](decisions/0005-experimental-facilities.md).
- 초록불 출발·지름길 미사용 반복 주행: [기초 데모](autonomy/BASIC_DEMO.md), [임시 C1 라이다](sensors/RPLIDAR_C1_SIMULATION.md), [결정 0006](decisions/0006-sensor-wall-following-demo.md).
- 2026-08-30~31 전체 구현 수준, 센서 배치·가격, 조향·회피·추월 선택지와 주최 측 질문: [2026-08-31 임시 보고서](reports/TEMP_REPORT_2026-08-31.md). 한 번 읽고 팀 논의에 쓰는 시점별 자료이며 상시 갱신 문서는 아닙니다.
- 2026-08-31 시설 작업에서 회의록을 다시 조회했으나 본문 접근에 실패했습니다. 시설 치수는 사용자 요청에 따른 실험 가정이며, 후속 공식 규정을 확인한 것으로 취급하지 않습니다.

## 최근 마무리 점검: 2026-08-31

- 동적 신호등, 임시 C1급 라이다, RGB 출발 판단·ArUco 벽 선택·라이다 본선 반복 주행을 구현했습니다. 제어 노드의 실제 구독은 RGB·`/scan`·`/clock`뿐입니다.
- 종료 경로 보완 뒤 최종 통합 실행에서 **2바퀴+약 2 m, 총 95.2856 m**를 진행하고 종합 통과했습니다. 초록 전 107개 궤적 표본의 명령·속도·변위를 별도 대조했으며 기록 표본상 움직임이 없습니다.
- 고정 20×15 cm 외형과 정적 벽의 겹침 0회, 최대 중심선 거리 약 0.1504 m, 마커 0/20/30/45와 양쪽 벽 선택, RGB·라이다·시계만인 제어 입력을 확인했습니다. 라이다는 500점·약 10.0001 Hz였습니다.
- 정지 요청 후 약 0.592초에 지속 정지하고 최종 오도메트리 속도 0을 확인했습니다. 부모 프로세스 종료 코드는 0/0, 오류 0, `shutdown_clean: true`였습니다. 앞선 종료 실패 보고서는 과거 실패 근거로 보존합니다.
- 검증기의 조기 출발 판정을 `run_started`와 독립하도록 보강했습니다. 최종 실행 도중 소스만 수정해 실행 중 검사기에 소급 적용되지는 않았고, 해당 실행의 출발은 위 원시 표본 감사로 보강했습니다. 새 판정의 회귀 검사를 포함한 자동 검사 62개, ROS 패키지 5개 빌드, 원본·실험 월드의 엄격한 SDF 검사를 통과했습니다.
- 같은 실행의 사선 조감도 한 바퀴 MP4를 `artifacts/videos/2026-08-31/basic_autonomy_overhead_one_lap.mp4`에 저장하고 디코딩·해시를 확인했습니다. 모든 검사·관찰 프로세스는 종료했으며 백그라운드 데모를 남기지 않았습니다.
- 현재 차량은 실물 디지털 트윈이 아니라 약 1.862 kg의 센서·제어 시험용 근사 모델입니다. 각 앞바퀴 관절도 0.45 rad로 제한될 때 이상적 뒤축 중앙 최소반경이 약 0.368 m라는 기하 제약을 새로 감사했습니다. 치수는 변경하지 않았습니다.
- D435i·C1·다중영역 ToF 배치/가격과 TTC→벽 추종/FTG→경주선/Pure Pursuit·Stanley→국소 계획/MPC·추월의 선택지를 임시 보고서에 비교했습니다. 구매·최종 알고리즘을 확정하지 않았습니다.
- 위 8/31 기능·검증·임시 보고서·조감도 영상을 기능 커밋 `d3cf0b0`으로 GitHub `main`에 반영했습니다. 원격 `main` 커밋과 보고서·MP4·미리보기·최종 검사 JSON·원시 궤적의 Git 블롭이 로컬과 일치함을 GitHub API로 확인했습니다.
- 현재 실험 월드의 본선 45 cm와 실제 20×15 cm 차량 SDF를 다시 대조했습니다. 긴 직선부에 차량 세 대를 폭 방향 15 cm 간격으로 놓으면 겹치지 않고 정확히 들어가지만 잔여 폭은 0 cm입니다. 높은 수직 카메라로 촬영한 설명·원본 사진과 배치 좌표는 [본선 폭 시각 확인](track/MAIN_WIDTH_VISUAL_CHECK.md)에 보존합니다. 이는 병렬 실주행 가능성이나 최신 공식 폭을 증명하지 않습니다.

## 목적

약 3개월 뒤 열리는 2026 IT ARENA 자율주행 대회에 GIST-ISAAC-Robotics 팀으로 참가하기 위해, 시뮬레이션을 우선 활용하는 소프트웨어 체계를 준비합니다. 전체 평면 외형은 20 cm x 15 cm로 팀이 확정했습니다. 나머지 하드웨어는 아직 설계 중이므로 `src/arena_description/config/vehicle.yaml`의 축거·윤거·바퀴 치수와 센서 배치를 바꿀 수 있도록 유지하면서 소프트웨어 실험을 먼저 시작합니다.

## 현재 파악한 팀·대회 조건

- 전달받은 차량의 평면 외형 제한은 20 cm x 15 cm이며, 팀은 이 범위를 최대한 사용하는 길이 20 cm·폭 15 cm로 제작하기로 확정했습니다. 완성 차량의 실측값은 아니며, 높이 규정은 아직 전달받지 못했습니다.
- 예선 성적에 따라 출발 위치를 정한 뒤, 약 5대가 동시에 출발할 예정입니다.
- 주최 측은 배터리, 전원 관련 보드, Jetson Orin Nano와 조향 서보를 제공할 예정입니다. 6월 30일 회의록에는 해당 전원 협업이 PDB(전원 분배 보드)로 논의되어 있으나, 실제 지급품의 최종 모델·사양은 별도 확인이 필요합니다.
- BLDC 모터를 이용한 구동이 허용됩니다. ESP32 계열 MCU와 구동륜 엔코더를 잠정적인 기본 구성으로 삼았지만, 정확한 ESC·모터·기어 구성·엔코더·통신 방식·전원 구조는 확정되지 않았습니다.
- 팀은 RealSense D435i를 보유하고 있으며 기본 센서로 사용할 계획입니다.
- 회전식 2D LiDAR와 선택적인 ToF 센서는 주로 주변 차량과의 충돌 방지를 위한 후보입니다.
- 출발 신호등을 카메라로 확인해야 합니다.
- ArUco 마커는 코스 구역과 분기 경로의 위치를 알려줍니다. 주행 경로를 선택하는 기능이 필요합니다.
- 2026-08-31 사용자가 전달한 새 회의 안건: 각 차량 후방에 **5×5 cm ArUco 식별 마커**를 부착하는 방안이 논의됐지만 아직 확정되지 않았습니다. 규정이나 현재 구현으로 취급하지 않습니다. 코드 영역과 흰 여백을 포함한 크기 중 무엇인지, 부착 높이·방향·사전·ID 배정은 전달받지 못했습니다.
- 첫 목표는 추월이 아니라 신뢰할 수 있는 감속·정지·충돌 회피입니다.

## 차량 치수의 확정 범위

- 전체 평면 외형: 길이 0.20 m·폭 0.15 m는 팀이 확정한 설계 목표입니다. 시뮬레이션의 기본 자세에서는 바퀴·센서·범퍼를 포함한 전체 외형으로 해석하며, 실제 검차의 측정 방식과 제작 공차는 별도 확인이 필요합니다.
- 검차 시 조향 상태: 사용자는 조향 중 바퀴 돌출은 검사하지 않을 것으로 예상하지만, 공식 조건은 아직 확인되지 않았습니다. 조향 중 전체 점유 영역이 20 cm x 15 cm 안에 있어야 한다는 규정을 추가로 가정하지 않습니다. 실차의 벽·주변 차량 충돌 판단에서는 조향 중 점유 영역을 고려해야 하며, 현재 주행 검사기의 고정 외형과 구분합니다.
- 임시 주행 치수: 사용자의 긴 축거·넓은 윤거 선호를 반영해 축거 0.145 m, 윤거 0.135 m로 늘렸습니다. 바퀴 반지름 0.025 m(지름 5 cm), 바퀴 폭 0.012 m는 유지합니다. 직진 상태의 바퀴 바깥 치수는 0.195 m x 0.147 m이며, 전체 목표 외형 안에 있습니다. 너클·허브·링크 등 실물 장착성은 미검증입니다.
- 조향 관절 한계 ±0.45 rad에서 바퀴의 보수적인 점유 폭은 약 0.1676 m입니다. 실차 충돌 판단에서 고려할 기하 계산값이지 현재 주행 검사의 외형 설정값이 아닙니다. 이번 주행 검사는 고정 20×15 cm 직사각형을 사용했습니다. 축거 증가에 따른 실제 최소 회전 반경·지름길 진입 궤적은 추가 검증해야 합니다.
- 높이 0.06 m와 차체 질량 1.5 kg, 조향 범위, 센서 위치·자세도 임시값입니다. 설정의 `footprint`는 전체 외형 목표이고, `body`는 그 평면 외형을 보수적으로 근사한 단순 충돌체이지 확정된 섀시 판 형상이 아닙니다.
- 지름길에 맞추기 위해 차량을 축소하지 않습니다. 주최 측 트랙의 기존 15 cm x 10 cm 차량 가정과 우리 차량의 확정 외형을 구분합니다.

## 2026-08-30에 확인한 컴퓨터 환경

- Windows 호스트 GPU: NVIDIA GeForce RTX 5070 Laptop GPU, 드라이버 610.74, VRAM 8 GB
- WSL: WSL2 기반 Ubuntu 24.04.4 LTS이며, WSLg와 NVIDIA GPU 연동을 사용할 수 있습니다.
- 최초 점검 당시 ROS 2와 Gazebo는 설치되어 있지 않았습니다. 현재는 ROS 2 Jazzy Desktop, Gazebo Harmonic / `ros_gz`와 관련 개발 패키지를 설치했으며, `scripts/doctor.sh` 점검을 통과했습니다.
- Windows에 Docker는 설치되어 있지 않았습니다. 첫 이정표에는 필요하지 않습니다.
- GitHub CLI는 `leejinh0225` 계정으로 인증되어 있습니다.
- 원격 저장소: `GIST-ISAAC-Robotics/2026-IT-ARENA` — 공개 저장소이며, 처음에는 비어 있었습니다.

## 아키텍처 결정

- PC 시뮬레이션: Ubuntu 24.04 + ROS 2 Jazzy + Gazebo Harmonic
- Jetson 배포: JetPack 6.2.1 / Ubuntu 22.04를 유지합니다. 시뮬레이터와 운영체제 버전을 맞추기 위한 목적만으로 재설치하지 않습니다.
- 가능한 범위에서 알고리즘 패키지가 ROS 2 Humble과 Jazzy 양쪽에서 소스 수준으로 호환되도록 유지합니다. 시뮬레이터 연동은 전용 패키지로 한정합니다.
- 환경이 달라져도 차량 제어·피드백 인터페이스는 유지하고, 이를 연결하는 어댑터만 교체합니다.
- 제어기 검증에는 Gazebo의 정답 위치·자세(ground-truth pose)를 사용할 수 있지만, 대회용 자율주행은 이에 의존해서는 안 됩니다.

선택 근거는 `docs/decisions/0001-simulation-platform.md`에 기록했습니다.

실제 차량의 잠정적인 역할 분담은 Jetson이 인지·계획을 담당하고, ESP32가 구동 장치의 동작 타이밍, 명령 수신 시간 초과 처리, ESC·서보 출력과 엔코더 신호 수집을 담당하는 구조입니다. `docs/decisions/0002-jetson-esp32-actuation-boundary.md`를 참고하십시오.

RGB와 깊이는 독립적인 Gazebo 카메라로 구성합니다. 기본 프로필은 각각 848×480의 60/90 FPS이고, 동일 주기 60/60·저부하 30/30도 지원합니다. 실제 장치와 공통으로 사용할 토픽은 유지하되, 영상 표현·정렬·점군 색상과 IMU의 차이는 어댑터 단계에서 처리합니다. 선택 근거는 `docs/decisions/0004-d435i-simulation-profile.md`에 있습니다.

사용자의 후속 요청으로 `demo.launch.py`에 RGB 신호 출발·ArUco 분기 선택·라이다 벽 추종을 추가했습니다. 기존 정답 위치 기반 중심선 추종 이정표와 별개인, 설명 가능한 센서 기반 시연입니다. 자율주행 입력은 `/scan`과 RGB뿐이며 지도·정답 위치·신호 제어 상태를 읽지 않습니다. 이 데모만 RGB 30 Hz·깊이 꺼짐을 기본으로 하며 일반 실행의 고속 설정은 유지합니다.

임시 RPLIDAR C1 기준 라이다는 360°·10 Hz·500점·0.05~12 m입니다. 스캔 중심 XYZ=(-0.03, 0, 0.125) m이며 차체를 피한 높은 위치의 벽 추종용 가정입니다. 저상 상대 차량 감지는 보장하지 않습니다. 실물 오차·반사율·회전 왜곡·거리 양자화는 미구현입니다.

## 트랙 원본

- 원본: `assets/track/original/it_arena_track_final.zip`
- SHA-256: `DE448BA10C614E0F635D44B2F36BAB29EBF455C323DE442562DD01A8296758E4`
- 압축 해제본: `assets/track/source/it_arena_track/`
- 원본 ZIP과 압축 해제 파일 23개를 바이트 단위로 대조하는 검사를 추가했습니다. 원본 자료는 수정하지 않았습니다.
- 실제 생성된 출력물과 동봉된 README의 내용이 서로 다릅니다. 반드시 `docs/track/TRACK_AUDIT.md`를 먼저 읽습니다.
- 2026-08-31 원본·실험 지도의 폐회로 중심선을 분석했습니다. 방위각 변화 폭 1° 이하의 최장 연속 구간은 두 지도 모두 약 10.3 m입니다. 장애물 없는 가시거리나 완전한 직선의 보장은 아닙니다. 계산 도구는 `scripts/analyze_track_straights.py`이며 형상은 변경하지 않았습니다.

## 계획한 소프트웨어 역할 분리

```text
Gazebo 월드·시뮬레이션 센서
              |
              v
시뮬레이션 어댑터 -> 공통 ROS 토픽 <- 실제 하드웨어 어댑터
                              |
                              v
       인지 -> 위치 추정 -> 경로·경주 상태 -> 계획 -> 제어
                                                      |
                                                      v
                                                 /drive 명령
```

Gazebo의 휠 관절 정답값은 `/sim/joint_states_raw`로만 노출합니다. 시뮬레이션 엔코더 어댑터는 정해진 엔코더 분해능에 맞춰 양자화한 `/wheel_states`와 `/wheel_encoder_ticks`를 발행하며, 이는 실제 ESP32에서 받을 피드백 인터페이스와 맞춥니다.

신호등·ArUco 인식 알고리즘은 렌더링된 RGB 영상을 입력으로 사용해야 합니다. 시뮬레이터 내부 상태의 직접 사용은 테스트의 정답 판정과 시각화에만 허용합니다.

## 검증을 마친 기초 구현

- 주최 측 원본 압축 파일을 수정하지 않고, 보존한 출력물을 엄격한 SDF 검증을 통과하는 실행용 월드로 변환했습니다. WSL에서 무리 없이 시작할 수 있도록 반복적인 정적 형상을 병합하여 링크 수를 1,090개에서 10개로 줄였습니다.
- 초기 주행·센서 검증에는 0.18 m x 0.12 m 크기의 임시 Ackermann 차량과 제공된 6개 출발 위치를 사용했습니다. 현재는 외형 0.20 m x 0.15 m·축거 0.145 m·윤거 0.135 m로 변경했고, 변경 후 검증 범위는 당일 활동 기록에 별도로 남깁니다.
- 원본 재현용과 별도 실험용 지도를 선택 실행합니다. 실험 지도는 본선 0.45 m·지름길 각각 0.25 m, 같은 중심선·누적 거리·출발 위치를 사용합니다. ID 30 표지판은 입구 간섭을 피해 실험 지도에서만 반대편으로 이동했습니다.
- 확대된 축거·윤거를 적용한 원본·실험 지도 모두에서 짧은 전진·조향·엔코더·RGB·깊이·IMU·명령 중단 정지를 확인했습니다. 차량·월드 SDF 검사와 자동 회귀 검사 22개가 통과했습니다. 종료 신호는 수신 콜백 사이에서 처리하도록 보완했으며, 시험 도구는 자식 프로세스의 종료 오류도 실패로 판정합니다.
- `/drive` 명령을 Gazebo에 전달하는 어댑터에 속도·조향 제한과 0.5 s 명령 수신 중단 감시 기능을 적용했습니다.
- `/odom`, `/tf`, D435i와 유사한 RGB·깊이·포인트 클라우드·IMU 출력을 ROS 2로 연결했습니다.
- Gazebo의 이상적인 관절 데이터는 `/sim/joint_states_raw`에만 둡니다. 매개변수를 조정할 수 있는 뒷바퀴 엔코더 피드백은 `/wheel_states`와 `/wheel_encoder_ticks`로 발행합니다.
- 창 없는 실행(headless)과 WSLg GUI 실행 모두 성공했으며, 종료 후 Gazebo·ROS 하위 프로세스가 남지 않았습니다.
- 환경 점검, 엄격한 SDF 검증, 트랙 원본 출처·무결성 검증, Python 인터페이스 테스트 4개, 직진·회전 동작, 카메라·깊이·IMU·엔코더 데이터 확인을 모두 통과했습니다.
- 2026-08-31 D435i RGB·깊이 분리 모델과 각 `CameraInfo`를 적용했습니다. 자동 회귀 검사 29개, 차량·두 월드의 SDF·원본 보존 검사, 고속/동일 주기/저부하 세 프로필의 센서·짧은 주행·정지·종료 검사를 통과했습니다. 기본 고속 프로필의 추가 재검사도 통과했으며, 실제 시간 기준 출력은 RGB 약 15·깊이 약 22 FPS였습니다. 실시간 성능 보장은 아닙니다.
- 시설 수정 전 실험 트랙과 차량의 실제 GUI 사진 3장은 `artifacts/screenshots/2026-08-31/`에 있습니다. 촬영은 화면 부하를 낮추는 `low_load_30` 실행 인수로 진행했으며 저장소의 고속 기본값이나 모델 형상은 바꾸지 않았습니다.
- 2026-08-31 후속 시설 수정: 실험본에 횡단 방향 신호등, 20×45×1 cm 코사인 방지턱, 독립형 ArUco 4개, 번호가 있는 출발 표시 6개와 체크무늬 피니시를 적용했습니다. 원본과 차량·카메라 치수는 바꾸지 않았습니다. 자동 검사 40개·두 월드 SDF·원본 23개 파일 보존 검사를 통과했습니다.
- 실제 차량 RGB로 출발 위치 6곳의 빨간 렌즈와 마커 4개의 세 접근 거리, 총 18/18 조건을 확인했습니다. 0.16 m/s 방지턱 통과에서 약 1.115 m 이동·차체 기준점 약 5.06 mm 상승·평지 높이 복귀를 확인했습니다. 당시 정지 안정성 실패 보고서는 보존하며, 이후 jerk 제한 수정 뒤의 시설/평지 지속 정지와 최종 통합 성공은 별도 실행 근거로 구분합니다.
- 기존 사진 3장은 시설 수정 전 기록입니다. 새 시설의 원본 RGB·검사 보고서는 `artifacts/screenshots/2026-08-31/facilities/`에서 구분합니다.
- 수정 후 실제 GUI의 `track_oblique.jpg`와 `start_grid.jpg`도 같은 시설 사진 폴더에 보존했습니다. 2026-08-31 최종 인계 당시에는 사용자의 요청대로 GUI를 사선 항공뷰·일시정지 상태로 열어 두었습니다. 카메라 구도와 해당 실행의 통신 영역은 [시설 안내](track/FACILITIES.md)에 기록했습니다.

## 단계별 이정표

1. 환경 점검과 의존성 설치를 반복 가능한 형태로 준비합니다.
2. 제공된 트랙을 창 없이 실행하고, Gazebo GUI에서도 불러옵니다.
3. 매개변수 기반 Ackermann 차량을 생성하고 수동 전진·조향 제어를 검증합니다.
4. 시뮬레이터의 정답 위치·자세를 이용해 제공된 중심선을 추종합니다.
5. 곡률 기반 속도 제어를 추가하고 단독으로 한 바퀴를 완주합니다.
6. 공통 인터페이스를 유지하면서 카메라·IMU를 추가하고, 이어서 2D LiDAR와 필요한 경우 ToF 모델을 추가합니다.
7. 신호등과 ArUco 마커를 렌더링하고 카메라 영상의 픽셀에서 인식합니다.
8. 정답값 입력을 실제 환경에 맞는 위치 추정 입력으로 교체합니다.
9. 다른 차량을 추가하고, 안전한 추종·정지와 제어 가능한 회피를 구현합니다.
10. 다중 차량, 잡음, 지연, 데이터 누락과 매개변수 범위별 반복 실험을 수행합니다.
11. 실제 센서·구동 장치 어댑터를 통해 동일한 자율주행 노드를 Jetson에서 실행합니다.

## 현재 미해결 사항과 위험 요소

- 최종 차량의 축거(wheelbase), 윤거(track width), 바퀴 반지름, 조향 범위, 질량, 무게중심(CG), 모터·ESC 응답 특성과 센서 장착 위치·자세를 아직 모릅니다.
- 실제 출력물의 분기 경로 폭 12 cm는 우리 차량의 확정 목표 폭 15 cm보다 좁아 통과할 수 없습니다. 원본은 15 cm x 10 cm 차량을 가정하고 있으므로 이전 규격이 남았을 가능성은 있지만, 주최 측의 오류로 확정하지는 않습니다.
- 본선 폭도 불일치합니다. 6월 30일 회의록은 차량 20 cm x 15 cm, 본선 노면 폭 45 cm와 바깥쪽 연석을 명시하지만, 현재 파일의 본선은 35 cm입니다. 이후 규칙 변경인지 생성 조건 누락인지는 확인되지 않았습니다.
- 생성된 트랙의 일반 구간 최소 곡률 반경 검사는 실패 상태입니다. 실제 값은 `0.2987 m`이며, 오래된 README에는 `0.45 m`라고 기재되어 있습니다.
- 트랙 README와 `output_final`은 한 바퀴 길이, 벽 높이, 마커 구성, 분기 형상과 어떤 자료를 기준으로 삼아야 하는지에 관해 서로 다릅니다.
- 원본 신호등은 세 등 모두 발광하며 UDP 스크립트가 화면을 바꾸지 않는 상태로 보존됩니다. 실험본 파일의 초기 상태는 등 중심 30 cm의 빨강 단독 점등입니다. `traffic_light:=true` 또는 데모 실행에서는 실제 렌즈 재질이 빨강→노랑→초록으로 바뀌며 RGB 인식으로 출발합니다. 대회 신호 주기·형상의 공식 확정값은 아닙니다.
- 실험 신호등은 현재 카메라로 6개 출발 위치에서 보이는 것을 확인했습니다. 실제 대회 신호등 높이·방향과 D435i 브래킷은 미확정이며, 원본의 약 1.08 m 높이 배치가 해결된 것으로 해석하지 않습니다.
- 이전 정지 속도 흔들림은 설치된 Gazebo Math의 jerk ±20 제한만으로도 재현했습니다. 속도 ±2·가속도 ±4 제한은 유지하고 jerk 제한만 제외했습니다. 후속 시설 재검사에서는 18/18 가시성·방지턱 통과와 함께 약 0.54초 안에 지속 정지 조건을 통과하고 최종 속도 0을 확인했습니다. 과거 실패 보고서는 덮어쓰지 않으며, 수정 근거는 결정 0006에 기록합니다.
- 2D LiDAR의 단일 스캔 평면은 그보다 낮은 상대 차량의 차체를 놓칠 수 있습니다. 최종 감지 범위를 결정하려면 상대 차량 높이에 대한 자료와 시뮬레이션 가림 테스트가 필요합니다.
- D435i 모델은 RGB 69.4°×42.5°·깊이 87°×58°의 명목 시야각과 별도 내부 파라미터를 사용합니다. 실제 보유 장치의 보정값은 아니며, 두 렌즈의 위치 차이·왜곡·깊이 결측·조명 민감도·롤링 셔터와 IMU 잡음은 미반영입니다.
- 848×480 깊이의 최소 거리는 0.195 m로 설정하고 기본 최대 절단 거리는 3 m로 둡니다. 4 m는 선택적인 확장 실험값이지 정확도 보장 거리가 아닙니다. RGB 렌더링 경계 20 m는 깊이 유효 거리·마커 인식 가능 거리와 다릅니다.
- 깊이 영상은 `32FC1`의 m 단위이며 실제 RealSense Z16 경로의 표현·스케일과 구분해야 합니다. RGB·깊이는 정렬되지 않았고, 기존 `/camera/depth/color/points`의 `rgb`는 실제 컬러 영상이 아닌 깊이 시각화 회색조입니다. Gazebo의 이상적 IMU 자세도 실제 센서의 직접 관측으로 취급하지 않습니다.
- 60/90 설정은 시뮬레이션 시간 기준입니다. 현재 PC에서 실시간보다 느리게 진행되고 일부 진단 실행에서는 영상 누락도 관측됐습니다. Gazebo의 정수 ms 주기 처리로 명목 60 Hz가 약 62.5 Hz로 생성되는 한계도 있습니다. 실행별 처리율·종료 결과는 2026-08-31 활동 기록과 검사 산출물에서 확인하며 실물 성능 보장으로 사용하지 않습니다.

## 본선·지름길 폭 실험 지도 — 적용 완료, 공식 규격 아님

- [2026-06-30 미팅](https://maddening-cause-ce7.notion.site/2026-06-30-38f99fd42e3080f6956fe5a5b90d0824)의 A-1·A-2에서 본선 노면 폭 0.45 m, 차량 0.20 m x 0.15 m 기준의 3대 폭과 지름길에 한정한 협소 구간을 확인했습니다. 지름길의 정확한 폭과 조향 시 검차 조건은 조회한 본문에 명시되어 있지 않습니다. 6월 당시 기록이며 최신 규정으로 확정한 것은 아닙니다.
- 사용자 승인에 따라 `config/tracks/experimental.yaml`에서 본선 0.45 m·지름길 각각 0.25 m를 관리하는 별도 실험 지도를 생성했습니다. 0.25 m는 공식 폭이 아닌 임시값입니다. `scripts/build_experimental_track.py`가 원본 생성기를 메모리에서 재사용하며, 원본 파일은 수정하지 않습니다.
- 실행 기본값은 `track:=experimental`이고 `track:=original`로 원본 재현용 0.35 m·0.12 m 지도를 선택합니다. `src/arena_gazebo/worlds/it_arena_experimental/`에 SDF·지도·CSV·설계 사본·출처 및 검증 기록을 함께 저장합니다. 출발 위치는 선택한 월드의 `scene.json`에서 읽습니다.
- 두 번째 지름길 입구의 ID 30은 간섭을 피해 실험 사본에서만 반대편으로 옮겼고, ID·경로상의 위치 `s`를 유지합니다. 후속 시설 수정에서 검은 코드 10 cm·흰 여백 포함 판 13 cm로 크기 기준을 명시했으므로 원본과 물리적 인쇄 크기까지 같다고 표현하지 않습니다. 마커의 정지 영상 가시성은 확인했지만 지름길의 연속 진입·합류 궤적은 미검증입니다.
- 차량 축소와 지도 전체 확대는 기본안으로 권장하지 않습니다. 차량 축소는 실제 충돌 여유·센서 위치·주행 특성을, 지도 전체 확대는 주 경로 폭·곡률·거리·마커 배치를 함께 바꿉니다.
- 본선 2,332개·지름길 155개와 141개 표본 및 출발 위치 6개에서 실제 SDF의 벽·저층 시설 661개와 차량 평면 외형 간 겹침이 없는 것을 확인했습니다. 이 정적 검사와 짧은 실행 검사는 완주·추월·다중 차량 안전성을 보장하지 않습니다. 결정 근거와 재생성 방법은 `docs/decisions/0003-track-variants.md`, `docs/track/EXPERIMENTAL_TRACK.md`에 있습니다.

## 다음 작업

1. 주최 측에 6월 30일 회의록과 현재 파일 중 어떤 폭이 최신 기준인지, 본선 45 cm의 유지 여부, 지름길의 실제 폭과 지급 PDB 최종 사양을 확인합니다. 상세 질문은 `docs/HARDWARE_AND_RULE_QUESTIONS.md`에 있습니다.
2. 하드웨어팀에서 실제 축거·윤거·안/바깥 최대 바퀴각·조향 중 점유 영역·질량/CG·서보/ESC/엔코더 응답을 측정합니다. 현재 차량 설정은 그 결과로 교체합니다.
3. 센서를 구매하기 전에 C1을 5/7/10/12.5 cm 스캔면에서, 다중영역 ToF 한 개를 3~5 cm 높이에서 낮은 검정/흰 모형·조향 가림·다중 능동 센서 조건으로 시험합니다.
4. 신호등·ArUco 인식을 다른 출발 위치·표지판 누락·다른 차량의 가림·노출·잡음 조건으로 확장합니다. 현재 데모는 실험 신호등 모양과 마커 ID별 벽 선택에 맞춘 최소 규칙입니다.
5. 현재 고정 거리 정지를 실측 제동거리와 TTC 기반 독립 안전층으로 보완하고, 정지차→느린 앞차 추종→cut-in 순으로 상대차 시나리오를 추가합니다. 경주선+Pure Pursuit/Stanley와 FTG는 같은 조건에서 비교합니다.
6. ESC, 엔코더, 정확한 ESP32 보드와 Jetson 통신 방식을 선택한 뒤, 기존 토픽 인터페이스에 맞는 실제 하드웨어 어댑터를 구현합니다.
7. 보유 D435i의 실제 스트림 조합·USB 연결·보정값·IMU 리비전을 조회하고, 노출·거리별 오차·지연을 측정합니다. 시뮬레이션의 지속적인 RGB-D·점군 처리율과 실물의 고속 처리 성능도 별도로 검증합니다.

## 문서 운영 원칙

- 프로젝트 설명과 인수인계 문서, 활동 내역, 결정 기록은 한국어를 기본으로 작성합니다. 필요한 전문 용어는 영문을 병기하되, 실행 명령어·코드 식별자·경로·ROS 토픽·메시지 형식·설정 키는 유지합니다.
- 이번 한국어판은 복구된 영문 문서에서 새로 번역했습니다. 앞서 작성된 번역본이나 그 백업은 참고하지 않았습니다.
- 번역과 문서 운영 원칙 추가는 별개의 작업입니다. 기존 기술 결정, 수치, 검증 결과와 미확정 사항은 번역 과정에서 바꾸지 않습니다.
- 주최 측 원본 트랙 자료는 번역 대상에서 제외하며, 한국어 문서와 같은 내용의 영문 문서를 별도로 중복 관리하지 않습니다.
