# Activity log: 2026-08-30

## Scope

Started the local 2026 IT ARENA autonomous-racing software and simulation project.

## Completed

- Read the referenced simulation-review conversation and recovered the intended ROS/Gazebo architecture.
- Inspected the Windows host, WSL, GPU forwarding, developer tools, and GitHub authentication.
- Confirmed WSL2 Ubuntu 24.04.4, WSLg, and NVIDIA GPU visibility from Linux.
- Confirmed ROS 2, Gazebo, and Docker were not installed at initial inspection.
- Found and cloned the empty public repository `GIST-ISAAC-Robotics/2026-IT-ARENA`.
- Preserved and extracted the supplied track ZIP; recorded SHA-256 `DE448BA10C614E0F635D44B2F36BAB29EBF455C323DE442562DD01A8296758E4`.
- Audited the actual `output_final` data and documented material conflicts with its README.
- Chose ROS 2 Jazzy + Gazebo Harmonic for PC simulation while preserving JetPack 6.2.1 on the Jetson.
- Added the project context, decision record, hardware/rule questions, provisional vehicle configuration, and future-agent handoff rules.
- Installed ROS 2 Jazzy Desktop, Gazebo Harmonic / `ros_gz`, development tools, and OpenCV ArUco support in WSL Ubuntu 24.04.
- Built a derived strict-SDF runtime world while preserving the supplied archive byte-for-byte. Merged repetitive static geometry from 1,090 links to 10 to reduce startup time.
- Added a parameterized 0.18 m x 0.12 m Ackermann vehicle, six-grid-slot spawning, `/drive` adapter, limits, and a 0.5 s command watchdog.
- Added a provisional D435i-like RGB-D camera and IMU; bridged RGB, camera info, depth, point cloud, and IMU ROS topics.
- Adopted a provisional Jetson / ESP32 split: Jetson owns autonomy; ESP32 owns command timeout, ESC/servo output, and left/right driven-wheel encoder acquisition.
- Added an encoder simulator with configurable resolution, rate, latency, and dropout. It publishes `/wheel_states` and cumulative `[rear_left, rear_right]` counts on `/wheel_encoder_ticks`.
- Replaced the shell-wrapped Gazebo launch path with tracked server / GUI child processes so Ctrl+C shuts down without leaving orphaned processes.
- Added four automated interface tests and a caller-independent environment doctor.
- Captured a WSLg Gazebo rendering under `artifacts/screenshots/gazebo_baseline.png`.

## Evidence and findings

- Host GPU: RTX 5070 Laptop GPU, 8 GB VRAM, driver 610.74.
- Actual track output: 46.6329 m lap, 0.35 m main width, 0.12 m branches, four physical ArUco IDs, 1.08 m lamp centers.
- Actual scene verification reports general minimum radius 0.298695 m and `min_radius_general_ok=false`.
- The included README describes materially different geometry and marker content.
- WSL doctor passed with ROS 2, colcon, Gazebo, rosdep, Python, RTX 5070 forwarding, and OpenCV ArUco detected.
- `gz sdf -k` accepted the derived runtime world.
- ROS package build completed for all four packages; encoder / command interface tests: 4 passed, 0 failed.
- Headless motion test at 0.30 m/s and 0.20 rad steering changed odometry to approximately `(x=0.782 m, y=0.111 m)` and yielded distinct cumulative rear counts `(9584, 11059)`.
- D435i-like IMU emitted orientation, angular velocity, and approximately `+9.8 m/s^2` vertical acceleration while stationary.
- RGB camera info / image width was 848 px; depth used `32FC1`; the point cloud width was 848 points per organized row.
- Both headless and GUI runs terminated cleanly; a post-shutdown process scan found no project Gazebo / ROS processes.

## Remaining / next

- Obtain organizer confirmation for the authoritative track/rules, especially 0.12 m branch width, signal behavior, marker specification, and supplied power hardware.
- Obtain hardware measurements and select the motor, ESC, ESP32, encoder, gearing, and link transport.
- Implement ground-truth centerline following and a repeatable solo-lap test.
- Add LiDAR / opponent-height experiments before buying additional ranging sensors.
